# 设计稿与原型（不参与构建）

这里放**不进构建产物**的设计参考：

- `master-nihongo-prototype/` — 早期网页原型（静态 HTML/CSS/JS）
- `Master Nihongo.dc*.html` — 界面设计稿导出

> ⚠️ 这些文件曾经放在 `frontend/public/` 下。`public/` 里的一切都会被 Vite
> 原样复制进 `dist/` 并随网页版公开发布，设计稿/原型不应该出现在那里。
> 需要预览时直接用浏览器打开本目录下的文件，不要移回 `public/`。
