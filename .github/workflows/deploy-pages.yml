name: Deploy ShuShuGo Pages

on:
  push:
    branches: [feat/fsrs-sync-accounts]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    defaults:
      run:
        working-directory: frontend
    env:
      VITE_SYNC_API_URL: https://master-nihongo-sync.master-nihongo-lsc.workers.dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
      - name: Build Zoo Lab
        run: npx vite build --config zoo-lab/vite.config.ts --base=/shushugo-app/zoo/
      - name: Build app
        run: npm run build -- --base=/shushugo-app/
      - name: Assemble Pages artifact
        run: |
          mkdir -p dist/zoo
          cp -R zoo-lab/dist/. dist/zoo/
          printf 'User-agent: *\nDisallow: /\n' > dist/robots.txt
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: frontend/dist
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
