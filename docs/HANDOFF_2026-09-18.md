# 交接：2026-09-15 ～ 09-18 这条线做了什么、怎么提交（2026-09-18）

> 写这份的原因：工作树里 195 个改动混着**至少两条线**（这条：辨析题 / 释义 / 动物园退场；
> 另一条：周报、语法拆分、例句音频、释义审计），而且写这份时另一条线还在改
> （`grammar-variant-splits.json`、`grammar.ts`、`study-core.ts` 都是 30 分钟内的 mtime，
> 两个测试正处在半截状态）。**在别人改到一半的树上提交等于替他定格一个半成品**，
> 所以这条线没有提交，把该怎么切留在这里。

## 状态

- `npx tsc --noEmit` 干净；`npm test` 708/710，挂的两个（`grammar-seed.test.ts`、
  `grammar-title-furigana.test.ts`）是另一条线今天的语法拆分（741 → 769 条）改到一半。
- `npm run build` 的前置 `verify-release-db.mjs` 挡着：`语法正文不一致: 条数 741 != grammar.ts 769`，
  同上，是语法拆分那条线要收尾的事（跑他们的 `apply-grammar-variant-splits.mjs` + 重烤）。
  `public/` 下那个 `.bak` 已挪到 `frontend/.local/`（gitignore），这一条不再挡。
- 出厂库 `public/nihongo.db` 是我 09-18 重烤的：包含辨析审校 1,559 条 + `sense_key` 列 +
  另一条线在 `jlpt_meaning_overrides.json` 里的 532 条释义审计（烤库读的是文件，分不开）。
  语法拆分那条线收尾时**会再烤一次**，届时以他们那次为准，内容不冲突。

## 这条线的文件（建议三个 commit）

### ① `feat(distinction): 辨析题、题面精修 7,592 条、审校同步进释义层、分组冻结键`

```
frontend/src/lib/distinction-quiz.ts            frontend/src/lib/distinction-quiz.test.ts
frontend/src/lib/distinction-candidates.test.ts frontend/src/pages/DistinctionQuizPage.tsx
frontend/src/pages/ConfusionPage.tsx            frontend/src/features/word-study/WordStudyPanels.tsx
frontend/src/lib/vocab-test.ts                  frontend/src/lib/vocab-test-distractors.test.ts
frontend/src/lib/auto-similar-meaning.test.ts   frontend/src/lib/confusion-groups.ts
frontend/src/lib/confusion-groups.test.ts       frontend/src/lib/models/word-card.ts
frontend/src/types/vocabulary.ts                frontend/src/lib/study-core.ts   ← 混：另一条线也改了
frontend/scripts/bake-seed-db.mjs               frontend/scripts/build-furigana.mjs   ← 混
frontend/src/data/question_meaning_overrides.json
frontend/src/data/jlpt_meaning_overrides.json   ← 混：含另一条线 532 条释义审计
frontend/src/data/word_sense_keys.json          frontend/public/nihongo.db   ← 混
question-meaning-review/**                      docs/DISTINCTION_QUIZ_PLAN.md
docs/DISTINCTION_QUIZ_PLAN_ROUND2.md            CLAUDE.md   ← 混：周报线也加了节
```

判据都在 CLAUDE.md：「辨析只有一份」下的「可练的辨析题」、「释义有两层」两节。

### ② `feat(app): 动物园主题退场，只留一只吉祥物`

```
删:  frontend/src/pages/ZooMapPage.tsx  ZooDexPage.tsx  HotSpringPage.tsx
     frontend/src/lib/zoo-badges.ts  zoo-badges.test.ts
改:  frontend/src/App.tsx   ← 混：周报线的路由也在里面
     frontend/src/components/ZooHome.tsx  AppNavigation.tsx  SquirrelTrail.tsx  ZooConfetti.tsx
     frontend/src/pages/TeamPage.tsx  SettingsPage.tsx  WordStudy.tsx
     frontend/src/lib/zoo-sounds.ts  studyPreferences.ts  grammar-quiz.ts(注释)
     frontend/src/types/app.ts  frontend/src/app.css   ← 混：app.css 本身是另一条线从 master-home.css 改名来的（未提交）
```

判据在 CLAUDE.md「动物园主题退场（2026-09-16）」。

### ③ `fix(dev): live-db 快照只收 5173 端口`

```
frontend/vite.config.ts   CLAUDE.md（顶部那条 ⚠️）
```

## 混着的文件怎么办

`App.tsx`、`app.css`、`study-core.ts`、`CLAUDE.md`、`jlpt_meaning_overrides.json`、`nihongo.db`
两条线都动了，按文件切不开。最省事的顺序：**先让另一条线把语法拆分收尾并提交**（他们的
commit 自然带上这些文件里他们的部分），再把上面①②③剩下的文件提交——那时这些混合文件
里剩的就只有这条线的改动。反过来先提交这条线也行，只是 commit 里会带上周报线的半成品。

## 这条线之外、顺手做的两处

- `jlpt_meaning_overrides.json` 里 `もっとも`「最；极其」→「合理，理所当然；不过（转折）」、
  `rock`「锁」→「摇滚；岩石」：另一条线释义审计里两条明显写错的，改了。
- 便宜模型跑第二轮时把 9 个老批次从一行一条改成了展开格式，diff 几千行，已恢复紧凑格式。
