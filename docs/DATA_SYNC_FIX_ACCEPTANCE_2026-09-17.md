# 数据同步修复与验收记录 · 2026-09-17

## 结论与范围

自查报告中六处失败路径已修复，并保留回归测试。代码位于隔离目录
`/Users/lsc/Documents/日语学习/开发中/shushugo-data-sync-fix-20260917`，基于
`37fe19cbb07769f899408e85c775febad223f677` 加原工作区既有改动。没有提交、推送、部署、写入个人学习库，
也没有刷新或操作 5173 学习页。实际浏览器验收使用 5189 和临时 Chrome profile。

本记录说明同步补丁的代码验收，不是整个项目已可发布的结论。全量小程序测试和生产构建仍有下面记录的既有内容阻塞。

## 修复闭环

| 原失败路径 | 最终行为 | 验证 |
| --- | --- | --- |
| 两标签覆盖同一增量 | 浏览器从首次读库开始持有页面生命周期 Web Lock；第二窗口在读库前停止，整库、增量、恢复和清理写入口均受保护 | 模块隔离测试及真实 Chrome 双页、IndexedDB 保存和重新打开 |
| 小程序旧快照覆盖新进度 | 四类记忆表持久保存版本和来源；本地更新盖版本、远端合并不重新盖章；同毫秒更新仍递增 | 新旧方向、重复导入、关闭重开后再次编辑 |
| 当前小程序快照被 Web 拒绝 | Web 保留 direction_tasks、achievement_unlocked 和方向卡旧评分/掌握字段；能力检查仍拒绝未知业务字段 | 当前两端真实导出器进行 mini → Web → mini → Web，逐项断言值和事件数 |
| 游客记录不能首次绑定账号 | 已明确选择手动上传时允许首次绑定；上传成功后才记 owner；其他账号数据仍在联网前拒绝 | 成功绑定、503 不绑定、generation=0、跨账号上传/拉取拒绝 |
| 语法取消永久掌握无效 | 语法同样持久保存和传递版本，较新 known_forever=0 生效 | 真实小程序快照合并断言 |
| 删除最后一条时长仍残留汇总 | 重建同时删除无明细日期，使用 savepoint 保证原子性；迁移识别墓碑，避免旧汇总复活 | 删除后重建、带陈旧汇总的存档重开、真实历史汇总迁移保留 |

同时补齐：跨表合并失败整体回滚；异步加载期间新作答保留；克隆设备的新事件随机 UID；
旧增量缺 UID 时稳定补齐；时间回拨触发整库保存；晚创建业务表后重新补同步结构；
权益到期后旧快照里的已知周报表不会阻断普通进度同步，未参与周报墓碑仍保留。
小程序导入 reviews 不再使用远端自增 id，避免两端不同事件恰好 id 相同而被忽略。

## 验证结果和复现命令

1. **前端全量**：`cd frontend && npm test`。**718 通过、25 跳过**，结果见 [日志](./data-sync-final-frontend.log)。原有 25 项 skip 保持原状态；本次新增的验收断言均未跳过。涉及两份完整词库和上万行合并的三个跨端测试单独给 15 秒预算；原先与全套测试争用 CPU 时超过默认 5 秒，未放宽业务断言。
2. **前端类型检查**：`cd frontend && npm run check`，见 [日志](./data-sync-final-typecheck.log)。
3. **浏览器实测**：隔离目录运行 `node frontend/scripts/sync-browser-smoke.mjs`（仓库根目录），脚本自动启动并关闭独立 5189 服务。脚本需要可用的 Playwright 和 Chrome；本机使用已有工具运行时，通过 `PLAYWRIGHT_MODULE_PATH` 指向其模块，没有给项目新增依赖。脚本端口固定为 5189、使用独立 Vite 缓存，拦截非测试 origin 请求。Chrome 152.0.7977.84 的实际结果见 [日志](./data-sync-final-browser.log)：第二页显示单窗口提示、不能写库，关闭第一页后恢复出 1 条刚保存的作答。
4. **Worker 全量及类型检查**：`cd cloudflare-sync && npm test && npm run check`，见 [日志](./data-sync-final-worker.log)。已将新的 `sync-route.test.mjs` 接入 npm test。测试调用真实 Worker fetch 路由，执行实际迁移和 SQL；D1 使用内存 SQLite 适配、R2 使用内存对象替身。验证空云端 generation=0、幂等重试、冲突、两个并发上传只能一个胜出、账号隔离、D1 失败后清理孤儿对象、保留最近三代、未授权拒绝及旧客户端格式保护。没有请求生产 Worker。
5. **小程序**：`cd wechat-miniprogram && npm test`。本机 Asia/Singapore 环境下前九项中的同步、快照、学习核心、语法均通过，运行到 confusion-smoke 失败。为了不漏掉后续检查，另逐个执行 package.json 所有 14 个脚本：**13 通过、1 失败**，见 [逐项日志](./data-sync-mini-all-scripts.log)。
6. **补丁格式**：`git diff --check`；交付补丁另执行 `git apply --check`，检查通过不代表已经应用。

浏览器脚本使用真实 Web Locks 和 IndexedDB，但不是 iOS/微信真机、设备被杀、线上账号和真实云服务的验收。
生命周期锁只能约束已运行新版代码的窗口；上线切换时旧代码窗口不懂这把锁，需在用户结束学习后正常关闭旧窗口再启用新版。没有为完成测试强制刷新当前学习页。

## 仍存在的项目基线阻塞

### 发布内容检查失败

`npm run build` 没有通过，记录见 [构建日志](./data-sync-final-build.log)：

```
语法正文不一致: public 边界串无效 160 条；iOS 边界串无效 160 条
```

先前阻塞 public 白名单的数据库备份，仅在隔离目录中移到 `local-build-backups/` 原样保留，
正式工作区该文件未动。随后上述内容检查继续失败。没有取消检查、改断言或把直接运行 Vite 当作完整发布构建通过。
隔离目录 `frontend/public/nihongo.db` 与正式工作区当前文件逐字节一致，SHA-256：
`125a36789163095969d487a76ed646f3e6a5021e9ed480227c55c429fe13b77f`。

### 小程序固定内容数量断言失败

`confusion-smoke` 期望 1912 组，现有内容实际 1719 组。该测试、`runtime/confusion.js`、出厂库均与正式工作区逐字节一致；
本次没有更改辨析内容或把固定数量改成当前值来通过检查。因此不能说小程序全套全绿。
另外试过 TZ=UTC，学习日测试因夹带 `+08:00` 时间而在 04:00 边界失败；本机时区运行学习核心通过，
这条测试本身仍有时区依赖，不是同步协议结论。

## 交付与合入

旁边的 `shushugo-data-sync-fix-20260917.patch` 只包含本任务相关源码、测试和说明。
`docs/data-sync-patch-baseline.json` 记录生成时正式文件和补丁目标的 SHA-256；源文件之后有新修改时须重新审查，
不要整目录覆盖，也不要将隔离目录所有既有脏改动一起提交。未纳入其他任务的 CLAUDE.md、音频脚本或内容数据变更。

安全合入时点是用户结束当前学习之后。需要先保留真实学习数据备份，核对基线与补丁，并处理以上内容发布阻塞；
iOS/微信真机、线上同步和最终发布须单独验收。本任务没有自动部署，也没有用定时任务安排未来写入。
