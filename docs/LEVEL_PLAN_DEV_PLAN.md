# 起点水平 → 备考计划：开发计划（2026-09-22 定）

> 2026-09-23 实施修订：原稿保留作为决策过程，具体实现与验收以文末「实施修订」为准。
> 原稿中「先验」是开发术语，产品里只说「自报水平的初始估计」；旧的「薄弱加量」、
> 本地专有计划状态、按第 8 天强推新词和未实现内容也让用户可选等条款已被本次决定取代。

原则（用户定的）：**系统可以复杂，但要替用户给出相对最优的默认，用户可以完全不管。**
第一次打开只问「现在哪一级、考哪一级」，剩下的算法算；算出来的计划要告诉他压力多大、什么时候最重，
明显不现实的给提示不拦。不再把「学习模式 / 题型」推到用户面前。

背景决策见 `CLAUDE.md`「起点水平 → 备考计划」一节；这份文件只讲**怎么做、按什么顺序、每步怎么验收**。

---

## 0. 常数表（所有数字集中在这里，改数只改这里）

| 常数 | 值 | 来源 / 理由 |
|---|---|---|
| `ANSWERS_PER_NEW_WORD_M1` | 8 | 作者库实测一个新词第一个月答 9.4 次（706 词），一般人顽固词少，取 8 |
| `SECONDS_PER_CARD` | 单词 12 / 语法 25 / 汉字 10 / 辨析 40 | `daily-plan.ts` 已有，不拿用户历史 |
| 巩固期 | `min(21, 窗口/4)` | 已做（`consolidationDays`），窗口锚 `jlptPlanStartedOn` |
| 新词 / 语法封顶 | 50 / 12 每天 | `MAX_DAILY_NEW_*` 已有；超了 = `feasible=false` |
| 起点先验：起点那一级 | 稳定性 30 天，到期均匀撒在前 14 天 | N4 = 876 ÷ 14 ≈ 63/天露面 |
| 起点先验：起点以下各级 | 稳定性 90 天，到期撒在前 30 天 | 几乎不露面，答对一次就是半年后 |
| 熟悉度滑块 5 档 | 0 → 没学过 · 25 → 3 天 · 50 → 10 天 · 75 → 30 天 · 100 → 90 天 | 对数；起点等级填默认（起点级 = 75，以下 = 100，以上 = 0） |
| 负荷档位（第 4 周稳态分钟/天） | ≤30 轻松 · 30–60 稳妥 · 60–90 压力不小 · >90 激进 · `feasible=false` 不现实 | 只有 ≥60 才在界面上说话 |
| 薄弱加量 | 近 7 天首答正确率 < 70% 且该题型 ≥ 50 题 → 最多 +40% **分钟**，从正确率 > 85% 的题型挪 | 分母是分钟不是题数（辨析 40 s、单词 12 s） |
| 送 Pro | 7 天，一个账号一次，计划创建那一刻 | 走云端 `trial` 权益 |
| 压力曲线 | 横轴 8 周，第一个学习周结束后才显示 | 用户定的 |

---

## 1. 数据模型

### 1.1 新的用户状态

| 键 | 存哪 | 同步 | 内容 |
|---|---|---|---|
| `starting_level` | `app_state` | lww，**要同步**（不是 device-local） | `"kana-none" \| "kana" \| "N5"…"N1" \| "beyond"` |
| `type_familiarity` | `app_state` | lww，同步 | `{ words, grammar, kanji, confusion }` 各 0–100 |
| `level_prior_applied` | `app_state` | lww，同步 | 先验写入的版本戳 + 起点 + 滑块值；写过就不再写 |
| `jlptPlanStartedOn` | `studyPreferences`（localStorage） | 不同步 | 已做。计划窗口锚 |
| `showJlptLevel` | `studyPreferences` | 不同步 | 已做，默认关 |

⚠️ `starting_level` 和滑块**只是先验**。两周后数据说了算：`learnedLevel()`（按数据算「现在 N 几」）继续留着，
备考一键的「现在」默认值从 `learnedLevel() ?? starting_level` 取。

### 1.2 先验行长什么样

先验写进各表的 FSRS 列，**不写流水**（`reviews` 只记真实作答）：

```
seen_count = 1, fsrs_state = 2 (Review), fsrs_stability = S, fsrs_difficulty = 5,
fsrs_due = 撒开的日期, fsrs_last_review = NULL, fsrs_reps = 0, fsrs_lapses = 0
```

先验行的签名 = `seen_count = 1 AND fsrs_reps = 0 AND fsrs_last_review IS NULL`。
需要区分「真学过」和「先验」的地方（覆盖率、成就、词库色阶）按这个签名排除，不加新列。

为什么 `seen_count` 要写 1：`fsrs-store.ts` 里 words 的 `eligible` 是 `seen_count > 0`，
0 的话进不了到期集、反而会被当新词抽走。代价是词库页上这些词显示成「学过 · 1–3 月」档，
覆盖率会跳到 100%——后者按签名排除，前者可以接受（它们确实在队列里）。

四张表都写：`progress`（词）、`grammar_progress`、`kanji_char_memory`、`confusion_progress`。
辨析组的先验按**最难成员**的等级（`level_rank` 已有），组里有成员没先验就不写。

### 1.3 只写一次，且只写还没学过的行

`applyLevelPrior()` 只碰 `seen_count = 0 AND known_forever = 0` 的行，写完把
`level_prior_applied` 记下来。重装 / 换设备：`app_state` 同步回来带着这个键，不会再写第二遍；
progress 行本身也同步了。用户改起点（备考页）→ 只对**仍然是先验签名**的行重算，真学过的不动。

---

## 2. 分步

每步独立可测、可单独合并；顺序是依赖顺序。

### Step 1 · 负荷模型（纯函数，无 UI）

`lib/plan/load-model.ts`

```ts
predictLoad({ newWordsPerDay, newGrammarPerDay, newKanjiPerDay, newConfusionPerDay,
              priorDuePerDay: { week1, week2 }, weeks = 8 })
  → { perWeek: { minutes, answers }[8], peakWeek, steadyMinutes, tier }
tierOf(minutes) → "light" | "steady" | "heavy" | "aggressive"
```

- 每周分钟 = Σ 题型（新/天 × 该周的「每个新词本周要答几次」曲线 × 秒数）+ 先验到期 × 秒数。
  曲线用 `ANSWERS_PER_NEW_WORD_M1` 按周拆：第 1 周 3、第 2 周 2、第 3 周 1.5、第 4 周 1.5、之后每周 0.5。
  （总和 8；第 4 周是 4 个 cohort 叠满的稳态峰。）
- 判据 `load-model.test.ts`：N4→N3 75 天（38 词 + 3 语法 + N4 63/天两周）第 4 周 ≈ 70 分 → heavy；
  五十音→N1 75 天 → 206 词/天 → infeasible；0 新词只有先验 → light。
- 先做它是因为 Step 3、6、7 都读它。

### Step 2 · 先验写入（纯数据，无 UI）

`lib/onboarding/level-prior.ts`

- `familiarityDefaults(startingLevel)` → 四个滑块默认值。
- `stabilityFor(slider)`、`spreadDays(slider)`。
- `applyLevelPrior({ startingLevel, familiarity })`：四张表各一条 UPDATE，按 1.2 的行形态；
  到期日撒开用 `word_id % spreadDays`（确定性，两端一致）。
- `priorSignatureSql` 导出给覆盖率 / 成就用。
- 写完 `requestFullSnapshot()`（改了几千行 progress，别等增量）+ `notifyProgressUpdated()`。
- 判据 `level-prior.test.ts`（出厂库）：N4 起点 → N5 888 + N4 876 行有先验、N3 一行没有；
  N4 的到期落在 14 天内且每天 ≈ 63；再跑一次一行不动；已答过的行（造一条 reviews）不被覆盖；
  改成 N3 起点 → N3 行加上、原 N4 行不动（仍是先验签名，稳定性改成 90）。
- ⚠️ 三处登记不用动（没有新表），但 `scripts/user-data-tables.mjs` 的出厂库守卫要确认 progress 的
  FSRS 列在出厂库里仍然全空。

### Step 3 · 备考一键升级 + 档位提示

`daily-plan.ts` 的 `examPreset` / `applyExamPreset`、`JlptPlanPage`

- `examPreset(current, target)` 返回值多带 `load: predictLoad(...)`；`current` 默认
  `learnedLevel() ?? starting_level`。
- 结果卡文案按档位：轻松 / 稳妥不说；压力不小 → 「第 N 周最重，约 X 分钟/天」；激进 → 加一句
  「明显超出常规，建议降一级或改考期」；不现实 → 已有的「按上限也吃不完，差 N 天」。
- **N4 双向**不用写规则：先验里忘掉的词进重学、到期数涨、`predictLoad` 的 `priorDuePerDay` 按
  实际到期重算 → 档位提示是实时的。要做的只有一条：`priorDuePerDay` 从库里现算，不用常数。
- 判据：`daily-plan.test.ts` 加「preset 带 load 且 tier 正确」。

### Step 4 · 两问 onboarding

`components/LevelSetup.tsx`（首启弹层 + 备考页「重新设定」共用一个组件）

- 第一行 chip：现在 —— 不懂五十音 / 五十音到 N5 / N5 / N4 / N3 / N2 / N1 / 专业进阶。
  默认 `learnedLevel() ?? "kana"`。
- 第二行 chip：考 —— N5…N1。默认 `jlptTarget`。考期默认下一场，可改。
- 「细调」折叠：四根 5 档滑块，默认 `familiarityDefaults(现在)`。
- 底部结果卡 = Step 3 的 preset 文案 + 「开始」。
- 点开始：写 `starting_level` / `type_familiarity` / `jlptTarget` / 考期 → `applyLevelPrior` →
  `applyExamPreset` → `refreshTodayWordPlan` + `refreshMixedCardTasks` → Step 7 送 Pro → 进学习页。
- 首启判据：`starting_level` 没写过且库里 `reviews` 为空（老用户不弹，备考页可以手动进）。
- 「不懂五十音」「专业进阶」在 Step 8 落地前：选了也能开始，但结果卡多一句
  「五十音教学还没上线，先从 N5 词开始」/「N1 以上内容还没有，按 N1 全量安排」。
- 判据：仓库没有 testing-library，组件不测；逻辑全在 Step 1–3 的纯函数里，组件只是接线。

### Step 5 · 薄弱加量

`daily-plan.ts` 加 `weakBoost()`

- 近 7 天各题型首答正确率（`reviews` / `grammar_reviews` / `card-log`），题数 < 50 的题型不判。
- 最低且 < 70% 的题型：其新学额度 × 1.4（按分钟折算成题数），差额从正确率 > 85% 的题型里按分钟扣；
  没有可扣的就只加不扣（总分钟涨，档位提示会跟着变）。
- 只在「一键安排 / 备考一键」和每天首次生成计划时算，用户手动拖过的段当天不动。
- 判据 `daily-plan.test.ts`：造 7 天流水，辨析 60% / 单词 90% → 辨析 +40% 分钟、单词按分钟扣掉等量。

### Step 6 · 压力曲线

`components/LoadCurve.tsx`，挂在备考页「每日学习量」下面、主页进度概览「每日」旁边

- 第一个学习周结束后才显示（`MIN(reviewed_on)` 距今 ≥ 7 天）。
- 数据 = `predictLoad` 用**今天实际**的新学额度 + 实际到期分布；8 根柱、峰值那根标「最重」。
- 一行字：「第 N 周最重，约 X 分钟/天；之后老词间隔拉长会回落」。
- 判据：纯函数在 Step 1 已测；这里只是画。

### Step 7 · 送 Pro（云端 trial 权益）

`cloudflare-sync/src/entitlement-rules.ts` + `index.ts`、`frontend/src/lib/purchases.ts`

- 新来源 `trial`：`entitlementStrength` 里排在「过期订单」之上、「有效订阅」之下；有到期；
  **同一原始交易可改写自己那行**的例外不适用（它没有交易），只允许被更强的覆盖。
- `POST /api/entitlements/trial`：登录用户、`trial_grants` 表按 user_id 唯一，`INSERT OR IGNORE` + 读回；
  已发过返回 409。没登录的设备不发（客户端把「送」延到登录成功的 `CLOUD_AUTH_EVENT`，和待校验购买同一套排队）。
- 迁移 `0015_trial_grants.sql`；`/api/health` 加 `migrations.trial`。
- 客户端：计划创建时调一次；文案按档位（0 节表）。
- 判据：`scripts/entitlement-rules.test.mjs` 加 trial 强度；路由测试：同账号两次 → 第二次 409；
  永久 Pro 账号发 trial → 权益不变。

### Step 8 · 内容：五十音 / 专业进阶（独立，可并行）

- 五十音：一张表（平 / 片，行列）+ 假名题（假名 → 罗马音四选一，反向），自己一份 FSRS 表
  `kana_memory`（照三处登记），一周的量。起点选「不懂五十音」时前 7 天只出它，词的计划从第 8 天起算
  （`jlptPlanStartedOn` + 7）。
- 专业进阶：没有内容就先不摆这一档；等有 N1 以上词表再加。

### 收尾

- 删掉 2026-09-22 加在圆环上的模式 chip 行（`DailyPlanPanel` 的 `zoo-plan-modes`）——用户不该再从这里选模式；
  圆环保留「看得到、能拖、一键回到计划」。
- 学习模式页保留，但主页大卡不再摆模式 chip。「题型面板 + 试一题」作为备选，等 Step 1–7 上线后看是否还需要。
- `docs/MIXED_STUDY_PLAN.md` 第 3 节末尾加一行指到这份文件。

---

## 3. 顺序与验收

```
Step 1 负荷模型 ──┐
Step 2 先验写入 ──┼─→ Step 3 备考一键 ─→ Step 4 两问 UI ─→ Step 5 薄弱加量 ─→ Step 6 曲线
                  │                                       └─→ Step 7 送 Pro（Worker 可先行）
Step 8 五十音 ────┘（独立）
```

每步合并前：`npm run check && npm run lint && npm test`；Step 2 之后在作者真实库上跑一次
`applyLevelPrior`（`MERGE_DB` 那种方式，只读副本），确认 N4 到期分布和 CLAUDE.md 里的数对得上。

⚠️ 作者的 dev server 开着时**不许**改会触发内容迁移的文件（`grammar_seed.json`、`study-core.ts` 的版本常量）。
这个计划不动它们，但 Step 8 建 `kana_memory` 表要进 `local-schema.sql`——建表本身安全，别顺手升种子版本。

---

## 4. 风险

1. **先验行污染统计**：覆盖率 / 成就 / 词库色阶把 1,764 个先验行当学过。按签名排除是每个读取方各改一处，
   漏一处就是一个假数。Step 2 交付时列出所有 `seen_count > 0` 的读取方并逐个判。
2. **先验和同步**：老设备没跑过 Step 2 的代码、收到带先验的 progress 行 —— 行形态是合法的 FSRS 行，
   老代码照常调度，无害。反过来新设备先验写完、老设备又推一份 `seen_count = 0` 的旧行 —— progress 按
   `sync_updated_at` lww，新的赢。
3. **负荷模型的 8 次是估的**：作者库 9.4，别人可能 6。曲线上写「预计」，第一周后用实测到期数替换预测。
4. **送 Pro 被薅**：只发给登录账号、一账号一次；删号重注是另一个账号——接受，7 天不值得再加闸。
5. **Step 8 五十音**是新内容线，量不小，别和 1–7 绑在一个版本里。

---

## 5. 2026-09-23 实施修订与验收口径

这节覆盖上文与它冲突的旧方案，不删除旧方案的推导，供以后追查取舍。

### 用户决定

- 产品文案只说「自报水平」「现在会从这些内容开始安排」，不向用户展示「先验」「验证」术语。自报等级先作为 FSRS 的初始估计；满两周以后，只有最近 14 天至少 50 道**真实首答**达到阈值，才最多上下调一级。样本不足就继续用自报值。
- **每日新词额度固定**，不会因错得多自动加量，也不会为了保证目标全覆盖把陌生卡不停塞给用户。原 Step 5 的 `weakBoost` 取消；正确率影响实际复习队列与两周后的起点建议，不改用户设定的新词额度。目标覆盖不了时只给诚实的可行性提示，不强制改计划。
- 五十音「约一周」是预计，不是第 8 天的开关。选「不懂五十音」后先学基础平假名、片假名，92 张基础假名卡各连续答对两次才解锁新词；完成当天记录真正的词计划起点，未完成就继续顺延。五十音答题进入独立 FSRS 记忆表和真实流水，跨设备合并后从流水重放。这个入门内容尚不包含浊音、拗音等完整假名课程，不把它写成完整五十音教学已验收。
- 「专业进阶」概念仍显示，但因 N1 以上内容不存在，选项写「内容准备中」并禁用，不把空白计划交给用户。
- 计划创建时尝试为已登录账号领取一次 7 天完整计划试用。领取失败就只安排单词，不排队重试；试用到期弹窗告知「现在只能背单词」，原计划里的语法、汉字、辨析额度保留，日后有 Pro 可继续使用。不做「游客领取」路径。

### 数据与算法纠错

- 起点记忆不能只改 `progress` 的 FSRS 列：汉字和辨析同步或撤销会从流水重放，把没有来源的初始状态清掉。新建 `level_prior_baselines`，记录四类卡的稳定性、难度、到期和虚拟起始复习时间；真实作答仍只写各自 `reviews`。重放从基线开始。改起点时只重算**没答过**的基线，已答过的卡连同它原来的基线一起保留。相同起点和熟悉度重复保存不重撒到期日。
- `fsrs_last_review` 在基线卡上要有有效的虚拟日期：由到期日减稳定性推导，并夹到当前时间之前。它不是用户作答事件；没有它，提前点卡会出现 FSRS 负天数错误。
- 基线需要 `seen_count=1` 才能进入现有复习队列，但首页、分析、备考覆盖率的「学过」必须排除仍只有基线的卡。真正答过或手动标熟知仍算学过。词库的队列色阶可表示「已在计划内」，不能拿它当真实覆盖率。
- 计划可行性按**剩余未学内容与可进新内容的天数**判断；进入巩固期还有未学内容时不能因今天新词为零而显示「可完成」。负荷曲线按今天额度和库里实际到期日估 8 周，**峰值从结果中求**，不能预设第 4 周。分钟只是粗估，不是用户作答速度承诺。
- `app_state` 随账号同步起点、熟悉度、目标、考期、计划起点、是否启用、每日四类新学/复习额度和五十音门槛；初始化基线和假名记忆表也随用户快照同步。合并后再把这些值恢复到本机 UI 偏好。`showJlptLevel` 等纯设备偏好继续本地保存。
- 跨设备的五十音最终作答可能只在流水合并后才达标：重放时记录真正完成日；若同步来的每日新词量仍是闸门期的 0，恢复偏好时从保存的原定额度解锁。试用到期清掉权益中的过期时间，弹窗时间标记单独保存，避免每次读权益都重复派发降级事件。
- 首页卡片、备考页与未来提醒只呈现当前能学的缺口：五十音进度门槛期间不催新词，无 Pro 不催语法；免费压力曲线只估单词。其它额度继续保存，恢复 Pro 后再参与显示与安排。
- 手动拉取备份须先检查本机数据归属；有另一个账号的数据或未绑定的学习数据时拒绝混入。手动上传读取当前云端代数做条件写，避免盲写覆盖。合并一批表、墓碑、流水重放和统计重建在一笔事务中完成，失败整批回滚。微信登录同时认 OpenID 与 UnionID 别名；两者已有不同用户时返回冲突，绝不静默合并账号。
- 云端 `trial_grants` 用账号主键防重复领取；重复请求返回第一次固定的到期时间，方便网络重试而不多送时间。试用弱于有效付费权益、强于过期订单；购买和退款仍以对应交易归属处理。`/api/health` 检查迁移是否已应用。

### 本地验收与发布边界

针对起点写入、FSRS 重放/撤销、五十音进度门槛、配置跨设备合并、负荷计算、可行性和试用路由都应有回归。前端依次运行 `check`、`lint`、测试、构建；Worker 运行 `check` 和路由测试；发布库守卫确认新用户表不会混入出厂库。**这些是本地验证**：云端 D1 迁移、真实账号领取、设备上弹窗与实际付款身份关联仍要在对应环境单独验收，不能由本地测试推出「已上线」。

正在学习时，本报告相关代码一律在隔离 worktree 操作，不刷新 5173，不让热更新打断学习。小程序、Android、iOS、Web 可能在不同分支；合入前重新核对各分支的改动与祖先，不能从当前分支的通过状态推断所有平台都已接入。

小程序共享层 `src/shared/web.js` 已在隔离工作树重建并通过 `check-shared`；小程序 `npm test` 中各项 smoke 运行到包体检查前均通过，最终因主包约 3.24 MiB 超过 2 MiB 而失败。改动前同一基线约 3.22 MiB，也已超限；需与正在进行的小程序包体改动合流后重新验收，不能把共享代码能构建误报成小程序可发布。

### 2026-09-23 首次设定交互修订

本报告前文 Step 4 的日期输入框、折叠“细调”和提交后“进学习页”是旧交互稿；当前按用户试用反馈修订，保留前文供追溯。首次设定页让用户直接选择当前水平与目标，考试场次使用可滚动日期轮（下一次、再下一次及后续场次），四类熟悉度直接展开。页面不展示“两周后按作答记录调整”等内部规则说明。按钮明确写“保存并查看今天怎么学”；提交后先进入备考页，首屏显示今天可做的任务与直接入口，不让用户从倒计时、负荷图和设置中寻找下一步。选“不懂五十音”时，首屏直接给假名练习，完成后才开放新词。

2026 年 12 月 6 日已由 [JLPT 官网](https://www.jlpt.jp/e/)公布；2027 年 7 月 4 日等后续日期是按惯例估算，界面必须标“预计”，官方公布后核对更新。考试场次在首次设定和备考页调整处使用同一个控件，避免一处可选、一处仍打不开日期输入框。深色主题下的浅色弹窗必须固定自身文字和控件颜色；窄屏内容可滚动，主要提交按钮始终可见。

### 2026-09-23 接手复核

- 固定的每日新学额度与系统上限是两道不同的可行性判断。原实现只在超过系统上限时提示「按每天的上限也吃不完」，但用户把每日新词设成较小的固定值时，界面仍可能显示计划可完成。备考页现按今天计算出的最低需求与当前固定额度比较，提示单词、Pro 语法各自的缺口，并明确不会自动增加额度。假名入门闸门期间暂不拿 `dailyGoal=0` 发这种提示；完成后会按当时剩余天数重算。
- 在隔离工作树运行的前端 `check`、`lint`、`build`、发布库守卫均通过；`lint` 为 0 错误、28 条警告。Vitest 并行执行时 752 个断言均通过，但收尾出现 42 个环境卸载错误，退出码为 1；改用 `npx vitest run --fileParallelism=false` 后 106 个测试文件、752 个断言通过，25 个既有跳过。备考相关 6 个文件、52 个断言单独通过。Worker `check` 与全套 `npm test` 通过。这些结果只覆盖隔离工作树的本地实现。
- 小程序主工作树正在独立推进，已把共享包移走大数据并使主包通过 2 MiB 预算；本工作树仍保留旧的生成脚本和约 3.24 MiB 包体。两处同源构建尚未合流，不能把这里的网页/Worker 检查当成小程序接入完成。主工作树还有大量未提交修改，合流前须逐文件检查两边实际 diff，避免覆盖学习页或小程序修复。5173 学习页不得刷新或收到本工作树的热更新。
- 「重新设定起点与目标」原来在完全相同的设置再次保存时也会把 `jlpt_plan_started_on` 重置为今天，悄悄改变巩固期和计划窗口。现仅在起点、四项熟悉度、目标或考期实际改变且调用方未显式给锚点时使用今天；相同设置保留原锚点。`level-plan.test.ts` 加了重复保存的回归，单文件 5 个断言通过。
- 用 5173 生成的 `.local/live.db` 做了只读副本验收：文件只通过 `readFileSync` 加载到 sql.js 内存数据库，绝不写回磁盘。快照里 N4 未学 149 词、N5 未学 60 词；给 N4 起点后，基线行数恰等于这些合格未学行，原有真实作答总数及已答词的 FSRS 状态逐行不变，N4 到期分散在 14 天内。对应可选测试是 `LEVEL_PLAN_LIVE_DB=/绝对路径/live.db npx vitest run src/lib/level-plan-live.test.ts`；不设置变量时跳过，避免 CI 依赖作者个人数据。
- 「已学辨析」范围原来直接用 `seen_count>0`，会把只有自报基线的词当真学过；现与首页覆盖率一致，只把真实正向作答、手动熟知或无基线的旧学习记录算进去，`distinction-quiz.test.ts` 加了基线与真实作答的对照。
- 备考一键原来按 `(自报起点, 目标]` 选新内容。若用户把起点本级熟悉度调到 0，该级的陌生词会被整个跳过。现按目标累计范围内仍 `seen_count=0` 的词、语法、汉字、辨析行取数；有基线的熟悉卡已进复习队列，自然不会再次作为新内容。`current` 参数因此从 `examPreset` / `applyExamPreset` 删除，相关测试更新。此后页面的物理上限提示和固定额度提示指向同一份真实未学池。

### 2026-09-23 小程序合流与本地验收续记

- 先核对了主工作区 `feat/fsrs-sync-accounts` 与隔离工作树 `codex/level-plan` 的 HEAD、脏文件和 diff；主工作区 `c9b1110` 上仍有小程序大量未提交改动，也有与本任务无关的 `frontend/src/lib/__tmp_probe.test.ts`。小程序改动按文件差异拷入隔离工作树、将其未跟踪的实际源码文件逐一补入，没有覆盖主工作区、没有把临时测试文件带入。已提交的 `c9b1110` 假名例词校验用 `git merge --ff-only` 合到隔离分支。主工作区仍保持原来的学习环境，5173 未刷新或热更新。
- 小程序共享入口加入网页 `level-plan.ts` 与 `kana-progress.ts`，`src/shared/web.js` 从隔离工作树的前端源码重建。小程序备考页接入网页同源的起点基线、目标累计范围、每日量预设和 92 张假名连续答对两次的闸门；微信页面只负责原生选项、存盘和路由。首次使用、且没有真实学习记录时，从小程序单词页进入设定；已有学习记录但未建计划的用户可在备考页主动开始。日期使用已公布场次与标明“预计”的后续场次，重新设定走同一个场次控件，不再保留旧的任意日期输入。
- 小程序固定额度不足时按新词、有效 Pro 的新语法分别提醒，假名闸门期不拿暂缓的 `dailyGoal=0` 误报；免费账户只催单词。已登录账号可调 Worker 的 `/api/entitlements/trial` 领取试用；无账号或领取失败时只排单词且不后台重试。试用过期后本机缓存立即失效，小程序学习页和备考页会提示一次、切回经典单词模式；混合模式入口对没有有效 Pro 权益的账号给出提示，避免继续安排付费内容。
- `wechat-miniprogram/npm test`：23 个脚本全部通过，包含新加的备考页模拟测试、五十音答题存盘、免费模式闸门、试用到期只提示一次、跨端同步和包体预算；主包 1.873 MiB，小于 2 MiB。前端 `check`、`lint --quiet`、串行 Vitest（106 文件、756 项通过、26 项跳过）、构建和发布库守卫通过；Worker `check` 与全部路由测试通过。合入 `c9b1110` 后重跑前端 `prebuild`，假名卡校验为 250 张通过。后续如果前端源码再变，必须重建共享包并复跑 `check-shared`。
- 本节验收是隔离工作树的 Node/网页本地验证。小程序页面的开发者工具视觉与交互、真机触摸/音效、真实账号试用领取、D1 `0015_trial_grants` 迁移、各平台购买归属及最终合分支/发布仍是单独门槛；不能据此宣称小程序真机或云端已验收。主工作区还有其他会话的未提交改动，合回去之前仍需再次对比两边最新 diff，禁止直接覆盖或批量暂存。
- 到期弹窗的“本机已看过”键只应属于设备；若跟 `app_state` 一起同步，一台设备确认后另一台会错过提醒。`sync/tables.ts` 将 `level_trial_expiry_noticed` 加入设备本地键，网页的同类标记仍在本地 `localStorage`。微信开发者工具的 CLI 编译尝试返回“服务端口已关闭”，本次没有改变它的安全设置；所以 Node 脚本通过不等于 IDE 已编译，需在开发者工具开启服务端口或手动打开隔离项目后补验。
- 继续检查发现“两周、50 道真实首答”的函数虽能算出调整后的级别，但此前只被一个已经不参与备考预设的辅助函数调用，实际未调整任何待学卡。现首次保存与再次打开备考页都对照自报值计算有效起点，只重算尚无真实作答的基线，原自报设置和已答卡不变；重复打开且结果不变时不重算。新增回归证明 49 道不足时仍按自报 N4，50 道正确首答后最多升到 N3，并写出 N3 未答卡基线。这个重估只在打开备考页时发生，不在每次答题后扫描整库；用户正常在跨页面学习后返回计划页时会应用最新证据。小程序共享包已重建并在最后再次跑包体检查，主包为 1.874 MiB；前端新单测文件共 6 项通过。此前 756 项是增加该单测之前的全量数，最终全量数需以收尾重跑为准。
- 完成收尾回归：`frontend/npx vitest run --fileParallelism=false` 为 106 文件、757 项通过，26 项既有跳过，**退出码 0**；`check`、`lint --quiet`、`build`（含出厂库与 250 张假名卡守卫）通过。中途全量测试的断言虽全部通过，Vitest 仍因 `word-api.modes.test.ts` 的测试用例在环境销毁后触发 `persistSoon()` 动态导入而报 42 个收尾错误、退出码 1；单跑该文件可过。测试桩现直接覆盖 `db-utils.persistSoon`，阻止测试写盘异步任务，随后全量以退出码 0 通过。小程序 `npm test` 23 脚本全部通过，最后一次 `npm run budget` 主包 1.874 MiB；Worker `check` 与全部 `npm test` 在 Worker 代码无后续修改的状态下通过。
- 用户在微信开发者工具开启服务端口后，CLI 成功打开隔离工作树的 `wechat-miniprogram`，开发者工具 Stable 2.02.2608070、基础库 3.16.3 完成普通编译。iPhone 12/13 (Pro) 模拟器显示首页到「JLPT 备考计划」的页面导航、未建计划时的「开始设定」入口，以及起点、目标、考期、四项熟悉度和底部固定保存按钮。视觉检查发现起点与等级按钮最初被排成竖列，已改为两列/五列网格；重新编译后确认横向排布，滚动到下半页可看到四项滑块及全宽的保存按钮。调试器中一次 `SyntaxError` 由人工在控制台输入错误命令造成，清除后为 0 个错误、0 个警告；没有在模拟器保存计划，避免改变其中已有的学习数据。这补上了本地 IDE 编译和首屏视觉验收，但不是触摸、音效、账号、云端或发布验收。

## 待办（2026-09-26，用户报告，小程序优先、这两条排后）

1. **老用户被当成新用户弹首次设定。** `b2b4a5e`（09-25，加非 JLPT 考试）把 `shouldShowLevelSetup()`（`frontend/src/lib/level-plan.ts:88`）改成「没有 `startingLevel` 就弹」。
   起点设定是 09-23 才有的，所以之前的用户全都没有这个值——作者用了两个多月，09-26 打开 5173 被弹了一张「新手等级」表。
   应改成：**没有 `startingLevel` 且没有真实作答（`reviews` 为空、只看真实流水，不算自报水平的基线）才弹**；
   老用户最多给一个不打扰的入口，起点默认用 `learnedLevel()` 从学习数据推，不让人从头选。作者本人已经填过，不会再弹。
2. **自动安排之后仍显示「低于建议」。** `DailyPlanPanel.tsx:148` 的判据是 `review < suggest.review || fresh < suggest.fresh`。
   作者 09-26 的截图：单词「新 40 · 复 511 · 低于建议」，新学已等于建议，所以是复习 511 小于建议复习数——
   自动安排写回的复习数和 `suggest.review` 不是同一个口径（复习上限 / 顽固词闸 / 减负和压轴的显示值 `wordExtras` 都可能是差额来源）。
   要查清两边各自怎么算，改成同一口径；自动安排的结果绝不应该被自己标成「低于建议」。
