# Legacy Backend

This FastAPI backend is an old sync prototype.

Production account, email verification, purchase entitlement, and cloud backup work now lives in:

```text
cloudflare-sync/
```

Do not add new production SQL or API work in this folder unless the project explicitly decides to revive the Python backend.
